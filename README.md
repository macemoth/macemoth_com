# macemoth.com

Run with `python3 render.py source static`.
Files in `source/blog` are treated as blog posts.

There is almost no parametrisation, messing with `render.py` is intended to keep things simple.
